উত্তর : যখন ভেরিয়েবল ডিক্লেয়ার করা হয় কিন্তু ভ্যালু এসাইন থাকে না তখন  undefined হয় এবং null হচ্ছে যার কোন মান নাই। ইচ্ছা করে ভেরিয়েবল ফাকা রাখা হয়
উত্তর : একটি অ্যারে থেকে নতুন অ্যারে তৈরি করতে map() মেথড ব্যবহার করা হয়।  একটি অ্যারে থেকে প্রতিটা element আলাদা করতে forEach() লুপ চালানো হয় 
উত্তর :  == শুধু মানের তুলনা করে কিন্তু === মানের সাথে টাইপ ও তুলনা করে
উত্তর :  async/await কোডকে synchronised করে। ডেটা না আসা পর্যন্ত অপেক্ষা করে। ইরোর হ্যান্ডেল করতে try/catch  করতে হয়
উত্তর : ভেরিয়েবল কোথায় অ্যাক্সেস করা যাবে তা নির্ভর করে scope এর উপরে। global scope ফাংশনের বাইরে ডিক্লেয়ার করা হয় এবং যেকোন জায়গায় থেকে এক্সেস করা যায়।  ফাংশন scpoe ফাংশনের ভিতরে ডিক্লেয়ার করা হয় এবং শুধু ফাংশনের ভিতর থেকেই এক্সেস করা যায়। ES6 এ {} এর ভিতরে let const ভেরিয়েবলকে block scope বলে
